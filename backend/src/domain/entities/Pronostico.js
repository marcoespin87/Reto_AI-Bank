// Entidad de dominio: Pronostico
// TODO: mapear campos desde tabla BD correspondiente en BASE.txt
class Pronostico {
  constructor(data) {
    Object.assign(this, data);
  }
}
module.exports = Pronostico;
